import React from "react";
import PropTypes from "prop-types";

function Header(props) {
  return (
    <React.Fragment>
      <h1> Keg Tracker</h1>
    </React.Fragment>
  );
}

export default Header;
