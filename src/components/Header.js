import React from "react";
import PropTypes from "prop-types";

function Header(props) {
  return (
    <React.Fragment>
      <div id="header">
        <h1> Keg Tracker</h1>
      </div>
    </React.Fragment>
  );
}

export default Header;
